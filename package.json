{
    "name": "my-project",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
      "minify": "for file in js/*.js; do terser \"$file\" -o \"${file%.*}.min.js\" -c -m; done",
      "deploy": "npm run minify && git add . && git commit -m 'update' && git push"
    },
    "devDependencies": {
      "terser": "^5.5.1"
    }
  }
  